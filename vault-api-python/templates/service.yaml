apiVersion: v1
kind: Service
metadata:
  name: {{ include "vault-api-python.fullname" . }}
  labels:
    app: {{ include "vault-api-python.name" . }}
    chart: {{ include "vault-api-python.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "vault-api-python.name" . }}
    release: {{ .Release.Name }}
